
<!-- ヘッダーのパーシャル -->
<nav class="navbar navbar-inverse header-size">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="collapsed navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-9" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-9">
      <ul class="nav navbar-nav">
        <% if signed_in? %>
          <li class="<%= 'active' if current_page?(current_user) %>">
            <%= link_to "マイページ", current_user %>
          </li>
          <li>
            <%= link_to "Sign out", destroy_user_session_path, method: "delete" %>
          </li>
          <li>
            <%= link_to "ユーザー情報編集", edit_user_registration_path %>
          </li>
       <% else %>
          <li class="<%= 'active' if current_page?(root_path) %>">
            <%= link_to "Home", root_path, class: "navbar-brand" %>
          </li>
          <li class="<%= 'active' if current_page?(new_user_registration_path) %>">
            <%= link_to "Sign up", new_user_registration_path %>
          </li>
          <li class="<%= 'active' if current_page?(new_user_session_path) %>">
            <%= link_to "Sign in", new_user_session_path %>
          </li>
        <% end %>

        <% if signed_in? && current_user.admin? %>
          <li class="<%= 'active' if current_page?(new_store_path) %>">
            <%= link_to "店舗登録", new_store_path %>
          </li>
          <li class="<%= 'active' if current_page?(stores_path) %>">
            <%= link_to "店舗一覧", stores_path %>
          </li>
        <% end %>
      </ul>
      <% if signed_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">退会処理<span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "退会する", current_user, method: :delete, data: { confirm: "本当に退会しますか？" } %></li>
          </ul>
        </li>
      </ul>
      <% end %>
    </div>
  </div>
</nav>
